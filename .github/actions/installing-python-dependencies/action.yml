name: Installing Python Dependencies
description: It will install python library
runs:
  using: "composite"
  steps:
    - run: python -m pip install --upgrade pip
      shell: powershell
    - run: pip install Flask matplotlib numpy pytest
      shell: powershell
