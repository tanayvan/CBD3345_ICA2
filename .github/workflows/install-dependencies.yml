name: Use Custom Action

on:
  push:
    branches:
      - main

jobs:
  use-custom-action:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Use Custom Action
        uses: owner/custom-action@v1.0
